import 'package:flutter/material.dart';
import 'presentation/screens/maze_board_screen.dart';

class MainGameEntry extends StatelessWidget {
  const MainGameEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const MazeBoardScreen(); // это твой начальный экран
  }
}
